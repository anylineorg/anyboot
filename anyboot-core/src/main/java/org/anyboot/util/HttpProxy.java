package org.anyboot.util;

public class HttpProxy extends org.anyline.util.HttpProxy{

	public HttpProxy(String host, int port, String user, String password) {
		super(host, port, user, password);
	}
}